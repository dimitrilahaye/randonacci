{
  "name": "randonacci",
  "version": "1.0.0",
  "description": "The essential API for your poker planning: designed for developers who never know what estimate to give. Bootstrapped with Fastify-CLI.",
  "main": "app.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "tap \"test/**/*.test.js\"",
    "local:start": "fastify start -l info app.js",
    "local:dev": "fastify start -w -l info -P app.js",
    "build": "echo build",
    "deploy": "gh-pages -d ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fastify/autoload": "^5.0.0",
    "@fastify/sensible": "^5.0.0",
    "fastify": "^4.0.0",
    "fastify-cli": "^5.7.1",
    "fastify-plugin": "^4.0.0"
  },
  "devDependencies": {
    "tap": "^16.1.0"
  }
}
